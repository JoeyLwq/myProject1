server:
  port: 12000
spring:
  application:
    name: my-config
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/joeylwq/myProject01.git
  rabbitmq:
    host: 192.168.101.134
management:
  endpoint:
    web:
      exposure:
        include: bus-refresh